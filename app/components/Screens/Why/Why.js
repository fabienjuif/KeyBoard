import React from 'react'

import MidSized from 'MidSized'

const Why = () => {
  return (
    <div>
      <MidSized colorName="gradiant5">
        Why we make KBOARD
      </MidSized>

      <MidSized colorName="gradiant4" textLeft>
        Imagine to build from A to Z
      </MidSized>
    </div>
  )
}

export default Why
